# Event sourcing example written in Go

This is an example project of an account service written in Go using the event sourcing pattern for persistence.
Apache Cassandra is used as the event database .
